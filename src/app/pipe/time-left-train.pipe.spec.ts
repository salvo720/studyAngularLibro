import { TimeLeftTrainPipe } from './time-left-train.pipe';

describe('TimeLeftTrainPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeLeftTrainPipe();
    expect(pipe).toBeTruthy();
  });
});
